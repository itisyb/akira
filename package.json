{
  "name": "akira",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start --stream --parallel"
  },
  "prettier": {
    "parser": "babel-ts",
    "semi": true,
    "singleQuote": false
  },
  "resolutions": {
    "**/**/dot-prop": "^5.1.1"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "prettier/prettier"
  }
}
