{
  "name": "akira",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "format": "yarn prettier --write",
    "prettier": "prettier --ignore-path .gitignore \"**/!(*d).+(ts|tsx|json|md)\"",
    "start": "lerna run start --stream --parallel"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn format"
    }
  },
  "resolutions": {
    "**/**/dot-prop": "^5.1.1"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lerna": "^3.22.1",
    "prettier": "^2.0.5"
  }
}
